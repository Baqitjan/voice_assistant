# =============================================================================
# LOQ-GENESIS | requirements.txt — ФИНАЛЬНАЯ ВЕРСИЯ
# Синхронизирован с рабочим окружением Бахыта
# RTX 4060 | CUDA 12.4 | Python 3.11 | Windows 11
#
# ВАЖНО: Порядок установки критичен! Используй install.bat
# =============================================================================

# ── PyTorch + CUDA 12.4 (устанавливать ПЕРВЫМ через install.bat) ─────────────
# pip install torch==2.6.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

# ── NumPy — ЖЁСТКО < 2.0 (numba + TTS + Silero требуют) ────────────────────
numpy==1.26.4
pandas==1.5.3

# ── Faster-Whisper (GPU транскрибация) ───────────────────────────────────────
faster-whisper==1.0.3
onnxruntime-gpu==1.18.1
ctranslate2==4.4.0

# ── Silero TTS (рабочий TTS, подтверждён логом) ──────────────────────────────
# Загружается через torch.hub — pip-пакет не нужен
# Зависимости:
soundfile==0.12.1
scipy==1.11.4

# ── XTTS v2 (клонирование голоса — опционально) ──────────────────────────────
# Transformers СТРОГО 4.45.0 (TTS 0.22 несовместим с 4.46+)
transformers==4.45.0
tokenizers==0.20.3
huggingface_hub==0.25.2
accelerate==0.34.2
TTS==0.22.0
coqpit==0.0.17
librosa==0.10.2
numba==0.59.1        # требует numpy<2.0 — зафиксировано выше
scikit-learn==1.3.2
audioread==3.0.1

# ── Аудио ─────────────────────────────────────────────────────────────────────
sounddevice==0.4.7
pyaudio==0.2.14

# ── Управление ПК ─────────────────────────────────────────────────────────────
pyautogui==0.9.54
Pillow==10.4.0
pywin32==306

# ── HTTP / Ollama ──────────────────────────────────────────────────────────────
requests==2.32.3

# ── Веб-поиск ─────────────────────────────────────────────────────────────────
duckduckgo-search==6.3.7

# ── Планировщик ───────────────────────────────────────────────────────────────
schedule==1.2.2
